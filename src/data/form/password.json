{
    "baseRoute": "user/me",
    "pathAfterSave": "/home",
    "singularName": "usuário",
    "pluralName": "usuários",
    "icon": "users",
    "title": "Usuário",
    "editTitle": "Trocar Senha",
    "form": [
        {
            "type": "password",
            "name": "password",
            "label": "Senha",
            "rules": [
                { "required": true, "min": 6, "pattern": "^(?=.*\\d)(?=.*[a-zA-Z]).{6,}$", "message": "A senha deve ter no mínimo 6 caracteres e conter letras e números" }
            ]
        },
        {
            "type": "confirm_password",
            "name": "password2",
            "label": "Redigite a Senha",
            "rules": [
                { "required": true, "min": 6, "pattern": "^(?=.*\\d)(?=.*[a-zA-Z]).{6,}$", "message": "A senha deve ter no mínimo 6 caracteres e conter letras e números" }
            ]
        }
    ]
}